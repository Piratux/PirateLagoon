#include "AudioEngine.h"
#include "GlobalAssetManager.h"

AudioEngine::AudioEngine() {
	//wave_engine.InitialiseAudio();
	//wave_engine.SetOutputVolume(0.1f);
}

void AudioEngine::play(AudioName audio_name) {
	//AssetManager* asset_manager = GlobalAssetManager::get();

	//wave_engine.PlayWaveform(asset_manager->get_audio(audio_name));
}